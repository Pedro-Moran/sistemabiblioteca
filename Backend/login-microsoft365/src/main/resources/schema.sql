BEGIN
   EXECUTE IMMEDIATE 'CREATE TABLE PROGRAMA (
       IDPROGRAMA NUMBER GENERATED BY DEFAULT ON NULL AS IDENTITY,
       DESCRIPCION VARCHAR2(255),
       ACTIVO NUMBER(1) DEFAULT 1,
       CONSTRAINT PK_PROGRAMA PRIMARY KEY (IDPROGRAMA)
   )';
EXCEPTION
   WHEN OTHERS THEN
       IF SQLCODE != -955 THEN
           RAISE;
       END IF;
END;
/
